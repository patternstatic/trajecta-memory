# Trajecta product site v1
User request: build the visible product site, after the private SDK delivery.

Direction: dark charcoal and sage/olive, restrained serif emphasis, existing Trajecta mark. Dark surfaces, readable muted text, and distinct amber rejection / green success states follow Ty's requested dark theme.
Use the existing competitive-distillation research: demonstrate the stale-handoff failure first, make branch/revision/next action visible, and give one obvious action.

Deliver: responsive product page, three illustrative browser workspaces, handoff approval and exact retry, activity history, downloadable clearly labeled sample receipts, beta guide and approved USD 9 offer. The sandbox is an explanation, not a connection to local SDK or AI accounts. Show the distribution status clearly.

Acceptance: desktop and mobile render; no horizontal overflow; every visible action works; stale rejection leaves revision unchanged, resume advances once, retry preserves receipt bytes, project switch/reset clears prior sample state, keyboard tabs and receipt export work.

Preview only in this turn. Preserve signed customer ZIP and private source boundary.

Verified: 11 focused site/model checks pass. Browser checks exercised stale rejection, approval, exact retry, activity, receipt JSON, downloaded receipt file, keyboard tab navigation, project changes/reset, FAQ and guide navigation. Mobile main page and guide have matching client/scroll widths (375px), with no horizontal overflow. Desktop viewport restored. Preview is served on http://127.0.0.1:4317/ and the tab is kept open for Ty. No backend/agent connection is claimed.
